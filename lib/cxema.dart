import 'package:flutter/material.dart';

class PokazCxemy extends StatelessWidget {
  const PokazCxemy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(
        body: Text("схема"),
      ),
    );
  }
}